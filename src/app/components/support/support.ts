import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-support',
  imports: [],
  templateUrl: './support.html',
  styleUrl: './support.css',
})
export class Support implements OnInit{
  constructor() { }

  ngOnInit(): void {
    // Standard initialization lifecycle hook
  }
}
